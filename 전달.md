스푼노트(SpoonNote)에 "지도 저장 목록(폴더) 링크 하나로 여러 장소를 한 번에 가져오기" 기능을 실제로 붙이려고 해.

전제:
- lib/parsers/naverPlacelist.ts, lib/parsers/googlePlacelist.ts에 폴더 링크 파서가 이미 작성되어 있음 (카카오도 있으면 lib/parsers/kakaoPlacelist.ts)
- 이 파서들은 기존 단일 링크 파서(lib/parsers/index.ts의 레지스트리, URL 하나 → 장소 하나)와 반환 형태가 다름(URL 하나 → 장소 배열)이라 아직 어디에도 연결 안 되어 있음
- 폴더 파서가 돌려주는 장소 데이터에는 기존 단일 링크 파서엔 없던 필드(사진 URL 배열, 카테고리)가 있을 수 있음

요구사항:

1. **URL 판별 로직**: 사용자가 보드의 "링크 붙여넣기" 입력창에 붙여넣은 URL이 개별 장소 링크인지 폴더(저장 목록) 링크인지 판별하는 함수 작성. 기존 naver.me/kko.to/maps.app.goo.gl 단축링크 처리 로직이 있다면 재사용하고, 최종 URL의 패턴(map.naver.com/v5/favorite/myPlace/folder/, google.com/maps/placelists/, 카카오 쪽은 조사 결과에 따라)으로 분기

2. **장소 데이터 스키마 확장**: 기존 place 데이터 모델에 photos(사진 URL 배열, optional), category(optional) 필드 추가. 기존 단일 링크로 추가된 장소는 이 필드 없이도 동작해야 함(하위 호환)

3. **API 라우트**: 폴더 링크를 받아서 해당 파서를 호출하고 파싱된 장소 배열을 반환하는 라우트 추가 (예: /api/boards/[boardId]/import-list). 페이지네이션이 필요한 파서는 내부에서 반복 호출해서 합치되, 최대 100개까지만 가져오고 100개 넘으면 잘렸다는 정보(총 개수, 가져온 개수)도 같이 반환. 비공식 API라 중간에 실패할 수 있으니, 이미 가져온 것까지는 살리고 에러 메시지와 함께 부분 결과를 반환

4. **미리보기/선택 UI**:
   - 폴더 링크를 붙여넣으면 로딩 상태 표시("N개 장소를 불러오는 중...")
   - 로딩 끝나면 카드 리스트로 미리보기 화면 표시 (사진 썸네일, 이름, 주소, 카테고리, 체크박스 - 기본 전체 선택)
   - "전체 선택 / 전체 해제" 토글 버튼
   - 이미 현재 보드에 있는 장소는(원본 place ID로 비교) 중복 표시하고 기본 체크 해제 상태로 시작
   - 100개 캡에 걸린 경우 "총 N개 중 100개까지 가져왔습니다" 안내 문구
   - "선택한 장소 추가" 버튼을 누르면 기존 "장소를 보드에 추가" 로직을 재사용해서 선택된 것들만 저장
   - API 부분 실패 시 이미 불러온 것까지는 보여주고 에러 배너 + 재시도 버튼 표시

5. 기존 보드 UI 톤(색/타이포/레이아웃)을 그대로 따라가고, 기존 "링크 붙여넣기" 입력창을 그대로 확장하는 방식으로 구현해줘 (별도 페이지로 분리하지 말고 같은 흐름 안에서 자연스럽게 이어지게).

작업 전에 기존 코드베이스의 보드 상세 페이지, 장소 추가 로직, 파서 레지스트리 구조부터 파악하고 나서 진행해줘. 완료 후 타입체크/린트 통과 확인하고, 어떤 파일을 만들고 고쳤는지 요약해줘.

---

## 구현 완료 (2026-08-27, Claude Code)

기존 코드부터 먼저 파악함: `AddEntryDialog`가 `PasteBox`를 품고 있고, `PasteBox`는
`/api/parse`(단일 링크)를 호출 → 결과를 `onParsed`로 부모에 전달하는 구조.
`entryService.addEntry`가 실제 저장 로직이고, `BoardDetailClient.handleAddEntry`가
그걸 감싸는 fetch 호출. 이 구조를 최대한 그대로 재사용하는 방향으로 설계함.

### 만든 파일
- **`lib/parsers/placelist.ts`** — `detectPlacelist(url)`: 리다이렉트 추적(기존
  `resolveFinalUrl` 재사용) 후 최종 URL 패턴으로 네이버/구글/카카오 폴더 링크
  판별. `PlacelistResult` 공통 타입(`{places, totalCount, partial}`)도 여기 정의
- **`app/api/boards/[slug]/import-list/route.ts`** — 폴더 링크 전용 라우트.
  개별 링크면 `{isPlacelist:false}`만 반환하고 끝(기존 `/api/parse` 흐름은 안
  건드림). 폴더 링크면 해당 파서 호출 → 100개 캡 적용 → 보드의 기존 엔트리와
  `sourceUrl` 비교해서 `isDuplicate` 표시 → 반환. 전용 레이트리밋(시간당 10회,
  `rateLimit.ts`에 추가) 적용
- **`components/entry/ImportListPreview.tsx`** — 미리보기/선택 UI. 사진 썸네일
  (없으면 📍)/이름/주소/카테고리/체크박스, 전체선택 토글, 중복 항목은 "이미 담김"
  배지+기본 체크 해제, 100개 캡 시 안내 문구, 실패 시 에러 배너+재시도 버튼(같은
  URL로 라우트 재호출)

### 고친 파일
- **`lib/parsers/{naver,google,kakao}Placelist.ts`** — 반환 타입을
  `ParsedPlace[] | null` → `PlacelistResult | null`로 통일(총 개수/부분실패
  여부 포함). 네이버·카카오는 장소별 실제 URL(예: `m.place.naver.com/place/{sid}`)을
  재구성해서 `sourceUrl`에 채움 - 기존엔 폴더 링크 하나를 모든 장소가 공유해서
  "이미 담긴 장소" 비교가 불가능했음. 네이버는 사진(`photos`)도 채움. 구글은
  장소별 안정적인 URL/ID가 없어서 폴더 링크를 그대로 씀(중복 판정 항상 false -
  알려진 한계로 남김)
- **`lib/types.ts`** — `ParsedPlace`/`Entry`에 `photos?: string[]` 추가(카테고리는
  이미 있었음), `ImportListPlace`/`ImportListResponse` 타입 추가
- **`lib/services/entryService.ts`**, **`app/api/boards/[slug]/entries/route.ts`**
  — `photos` 필드 저장/전달 지원
- **`lib/constants.ts`** — `LIMITS.importListMax = 100`
- **`lib/rateLimit.ts`** — `RATE_LIMITS.importList` 추가
- **`components/paste/PasteBox.tsx`** — `boardSlug`/`ownerKey`/`onParsedList` prop
  추가(선택적). `boardSlug`가 있으면 제출 시 import-list 라우트를 먼저 호출해서
  폴더 링크인지 확인 → 폴더면 `onParsedList` 호출, 아니면 기존 `/api/parse`
  흐름으로 그대로 폴백. 홈페이지의 기존 `<PasteBox />`(보드 컨텍스트 없음)는
  동작 변화 없음
- **`components/entry/AddEntryDialog.tsx`** — 폴더 링크 감지되면 기존 단일
  입력 폼 대신 `ImportListPreview`를 보여주도록 분기. "선택한 장소 추가"는 새
  `onAddMany` prop으로 위임
- **`components/board/BoardDetailClient.tsx`** — 기존 `handleAddEntry`의 실제
  네트워크 호출을 `postEntry`로 뽑아내고, 그걸 그대로 재사용하는
  `handleAddManyEntries`(순차 호출, 실패 시점까지 성공 개수+에러 메시지 반환)를
  추가해서 `AddEntryDialog`에 연결

### 설계 결정 / 알아둘 점
- **API 라우트를 하나로 합치지 않고 분리함**: `import-list`는 폴더 링크
  판별까지만 책임지고, 아니면 클라이언트가 기존 `/api/parse`를 또 호출함(왕복
  1번 추가). `/api/parse`를 안 건드려서 회귀 위험을 줄이는 쪽을 택함 - 보드
  화면에서만 나는 왕복 1번 정도는 감수할 만하다고 판단
- **"원본 place ID로 비교"는 `sourceUrl` 재사용으로 구현**: 새 필드를 추가하는
  대신, 폴더 파서가 장소별로 실제 개별 링크를 재구성해서 `sourceUrl`에 넣게
  고쳤음. 기존 단일 링크 추가 흐름과 완전히 같은 필드로 중복 판정이 가능해짐
- **총 개수(`totalCount`)는 "정확한 진짜 총량"이 아니라 파서가 파악한 범위 내
  최선치**임(네이버는 페이지네이션 200개 자체 상한 있음) - 실사용 규모에선
  문제 안 될 걸로 보임
- **"N개 불러오는 중" 로딩 문구는 못 넣음** - 응답이 오기 전엔 개수를 알 수
  없어서, 그냥 기존 스피너로 대체함
- **버그 하나 발견·수정**: `detectPlacelist`의 구글 패턴이 처음엔 브라우저
  UA 기준 URL 모양(`/maps/placelists/`)만 잡았는데, 실제 리다이렉트 추적에
  쓰는 봇 UA는 다른 URL 모양(`/maps/@/data=...!11m1!2s{id}`)으로 도착해서
  구글 폴더 링크가 전부 `isPlacelist:false`로 오판정되고 있었음. 실제
  dev 서버로 세 서비스 링크 전부 재현 테스트하다가 발견해서 패턴 두 개 다
  잡도록 수정

### 검증
- 타입체크(`tsc --noEmit`)/린트(`eslint .`) 전부 통과
- 실제 dev 서버 + 진짜 보드로 end-to-end 테스트: 네이버(27개, 사진 포함)/
  구글(3개)/카카오(1개) 폴더 링크 전부 정상 파싱, 개별 장소 링크는
  `isPlacelist:false`로 정상 폴백 확인
- 이미 담긴 장소 중복 판정(재수입 시 "이미 담김" 배지+기본 체크 해제) 확인
- Playwright로 실제 화면에서 붙여넣기→미리보기→"선택한 장소 추가"까지 눌러서
  보드에 26개 일괄 반영되는 것 확인(스크린샷으로 레이아웃도 확인)
- 50개 상한 초과 시나리오도 재현: "0개 추가 후 멈췄어요: 보드 하나에는 최대
  50개까지 담을 수 있어요." 에러 배너가 뜨고 모달이 안 닫히는 것까지 확인
- 테스트로 만든 보드는 삭제 정리함