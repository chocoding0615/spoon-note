프롬프트 10 — 하단 탭 구조 개편 (홈/커뮤니티/마이페이지)
스푼노트 앱의 내비게이션을 하단 탭 3개(홈/커뮤니티/마이페이지)로 재구성하려고 해. 당근마켓 앱의 하단 탭 UX(아이콘+라벨)를 참고 레퍼런스로 삼았어. (계정 시스템은 별도 작업으로 이미 진행됐거나 진행 중이라는 전제 — 로그인 상태를 반영할 수 있으면 반영해줘)

요구사항:
1. 하단 고정 탭바: 홈 / 커뮤니티 / 마이페이지, 각각 적절한 아이콘

2. 커뮤니티 탭: 기존 커뮤니티 피드 페이지를 이 탭 아래로 연결. 정렬 옵션은 다음 4개로 구성:
   - 인기순: 보드에 담긴 장소들의 canonical place 찜 카운트 합산 기준
   - 거리순: 사용자 위치(또는 마지막 선택 지역) 기준 가까운 순
   - 최신순: 보드가 커뮤니티에 등록된 시각 기준
   - 담아간 횟수순: 다른 사용자가 "담아가기"한 총 횟수 기준
   조회수·좋아요 정렬은 트래킹 기능이 아직 없으니 이번엔 만들지 마. 필요하면 이후 별도 작업으로 추가

3. 마이페이지 탭: 상단에 로그인 상태에 따라 로그인/회원가입 버튼 또는 프로필 표시. 그 아래로 기존 기능들(지도 링크 붙여넣기로 보드 만들기, 내 보드 목록, 설정 등) 배치

4. 홈 탭: 아래 구성을 기본안으로 제시하지만, 더 나은 아이디어가 있으면 꼭 이대로 만들지 않아도 되고 대안을 제안해줘(제안 내용은 최종 요약에 포함):
   - 상단 지역 선택(현재 위치 또는 마지막으로 본 지역)
   - "OO동 맛집 랭킹 TOP 5" 미리보기 (기존 지역 랭킹 페이지 데이터 재사용, "더보기"로 전체 랭킹 이동)
   - "이번 주 급상승" 위젯: 최근 며칠간 찜 카운트가 많이 증가한 장소. 이 로직은 새로 설계 필요(예: 찜 추가 이벤트를 타임스탬프와 함께 기록해서 최근 N일 증가량 계산)
   - 에디터 큐레이션 배너 자리: 지금은 정적 슬롯만 두고 실제 콘텐츠 연결은 이후 작업
   - 검색창(장소/지역/카테고리 검색)
   - "지도로 보기" 진입점: 지역별 찜 개수를 시각적으로 보여주는 지도 화면(프롬프트 11에서 별도로 만듦)으로 이동하는 버튼/카드

기존 디자인 톤을 유지하고, 완료 후 build/lint 통과 확인해줘. 최종 요약에 홈 탭 구성에 대해 제시한 대안이 있다면 꼭 포함해줘.

---

## 구현 완료 (2026-08-27, Claude Code)

기존 코드부터 파악: 하단 내비게이션/헤더 자체가 없었고(`app/layout.tsx`가 폰트+`<body>`뿐),
커뮤니티 피드는 정렬 없이 최신순 고정, `/my`는 이미 로그인패널+내 보드 목록 구조라 요구사항 3과
거의 맞아떨어짐. 아이콘 라이브러리는 애초에 없어서(기존에도 🥄/🔥/메달 이모지로 통일) 탭 아이콘도
이모지로 감.

### 만든 파일
- `components/nav/BottomTabBar.tsx` — 홈🏠/커뮤니티👥/마이페이지👤 고정 탭바(client, `usePathname`으로 활성 표시). 로그인 상태 반영: 마이페이지 아이콘을 로그인 시 프로필 사진으로 교체(사진 없으면 우측 상단에 작은 점 배지)
- `components/home/{RegionPicker,SearchBar,TrendingWidget,RankingPreview,CurationBannerSlot,MapEntryCard}.tsx`, `app/map/page.tsx`(프롬프트 11 전 자리표시 페이지), `app/api/search/route.ts`
- `components/community/SortSelect.tsx`, `lib/utils/geo.ts`(haversine 거리 계산 - matching.ts의 근접 매칭과는 별개 용도라 공유 안 함)

### 고친 파일
- **`app/layout.tsx`** — async로 바꿔 `getSession()` 한 번 읽어 `BottomTabBar`에 내려줌. `body`에 `pb-16`으로 탭바 높이만큼 여백
- **`app/page.tsx`(홈 탭 전면 개편)`**, **`app/my/page.tsx`**(요구사항 3대로 `PasteBox`+"보드 만들기"를 홈에서 여기로 이동), **`app/community/page.tsx`**(정렬 4종 추가), **`components/community/{RegionSelect,FeedCard}.tsx`**(preserveParams로 지역↔정렬 파라미터 서로 안 지우게, 정렬별 근거 한 줄 표시)
- **`lib/types.ts`** — `Board.communityAt`(커뮤니티 등록 시각, "최신순" 기준 - board.createdAt과 별개), `CanonicalPlace.collectCount`(담아간 총 횟수, saveCount와 별개 카운터), `CollectiblePlace.canonicalId`, `FeedSort`, `FeedBoardCard` 확장(totalSaveCount/collectedCount/lat/lng/communityAt), `PlaceSaveEvent`/`TrendingPlace`/`RegionCentroid` 신규
- **`lib/services/canonicalPlaceService.ts`** — `recordPlaceSave` 반환값을 `string` → `{canonicalId, isNewSave}`로 변경(호출부 3곳 전부 수정), `incrementCollectCount`/`getCollectCounts`/`listTrendingPlaces`/`listRegionCentroids`/`searchPlaces` 추가, 새로 찜될 때마다 `placeSaveEvents` 컬렉션에 타임스탬프 기록(fail-open)
- **`lib/services/{entryService,boardService,feedService}.ts`** — `collectEntry`가 성공 시 origin 장소의 `collectCount` 증가, `createBoard`/`updateBoard`가 `communityAt` 기록, `listCommunityFeed`가 `{region,sort,origin}` 옵션 받아 4가지 정렬 지원
- **`app/api/boards/[slug]/collect/route.ts`**, **`components/board/BoardDetailClient.tsx`** — "담아가기" 요청에 origin `canonicalId` 실어 보내서 담아간 횟수 집계
- `lib/constants.ts` — `FEED_SORT_OPTIONS`, `TRENDING`, `HOME_RANKING_PREVIEW_LIMIT`, `LAST_REGION_STORAGE_KEY`
- `lib/services/canonicalPlaceService.test.ts` — `recordPlaceSave` 반환 타입 변경 반영 + `placeSaveEvents` 테스트 데이터도 정리하도록 `afterEach` 보강(실제 Firebase 프로젝트에 붙는 통합 테스트라 안 지우면 테스트용 가짜 장소 이름이 급상승 위젯에 며칠 섞여 들어감)

### 설계 결정
- **"인기순"과 "담아간 횟수순"은 완전히 다른 카운터**: saveCount(몇 개의 커뮤니티공개 보드가 이 장소를 가졌나, 비공개 제외)와 별개로 collectCount(담아가기 버튼이 실제로 눌린 횟수, 담는 보드 공개설정과 무관)를 신설. 담는 보드가 비공개여도 "담아간 횟수"는 올라가야 해서 destination 쪽 재매칭이 아니라 origin canonicalId를 클라이언트가 그대로 실어 보내는 방식으로 구현
- **"거리순"**: 브라우저 geolocation 권한 요청 → 좌표를 URL(`lat`/`lng`)에 실어 서버가 haversine으로 정렬. 권한 거부/미지원 시 서버가 "최신순"으로 폴백(지역 필터가 이미 걸려있으면 사실상 "이 지역 최신순"과 비슷하게 동작 - 완전한 "마지막 선택 지역" 로직까지는 아님, 필요하면 후속 작업)
- **"이번 주 급상승"**: `recordPlaceSave`가 saveCount를 실제로 늘리는 순간마다 `placeSaveEvents`에 타임스탬프 1건 기록(신규 컬렉션) → 최근 7일 이벤트를 canonicalId별로 세어 상위 5개. 트랜잭션 밖에서 fail-open으로 처리해 핵심 동작(찜 저장) 안정성에 영향 없음
- **홈 탭 "현재 위치로 찾기"**: 역지오코딩 API 없이, canonical place들의 지역별 평균 좌표(centroid)를 서버에서 미리 계산해 내려주고 클라이언트가 가장 가까운 지역을 고르는 방식으로 구현(정확한 행정동 경계는 아니지만 "대충 가까운 동네" 목적엔 충분)
- **검색은 MVP 수준**: Firestore가 전문검색을 지원 안 해서 placeName/category 접두어(prefix) 매칭만 지원("마라"는 "마라탕"엔 걸리지만 "동대문마라탕"엔 안 걸림). 아직 장소 상세 페이지가 없어서 검색 결과는 지역 랭킹/커뮤니티 피드로 연결. 실사용 데이터가 쌓이면 Algolia 등 외부 검색엔진 도입이 다음 단계

### 홈 탭 대안 제안 (요구사항 4)
기본안 그대로 구현했지만, **"지도 링크 붙여넣기로 보드 만들기"가 마이페이지 탭으로 이동한 것(요구사항 3)이 실은 홈 탭 설계와 충돌 여지가 있다**고 생각함 - 지금 첫 방문자가 앱을 열면 홈 탭(피드/랭킹 위주)이 보이고, 정작 이 앱의 핵심 액션인 "링크 붙여넣기"는 한 탭 건너(마이페이지)에 숨어있게 됨. 요구사항을 문자 그대로 따라 일단 이렇게 구현했지만, 다음 중 하나를 고려해볼 것을 제안:
1. 홈 탭 최상단(지역 선택 위 또는 검색창 자리)에 "링크 붙여넣기" 입력창을 얇게 하나 더 두고, 실제 파싱/보드 배정은 마이페이지의 전체 플로우로 넘기는 절충안
2. 탭바 가운데에 당근마켓 "글쓰기"처럼 살짝 튀어나온 "+" 버튼을 추가해 홈/커뮤니티 어디서든 바로 보드 만들기로 진입(당근마켓 참조 레퍼런스와도 UX 결이 맞음)
지금은 요구사항 3을 문자 그대로 따라 마이페이지에만 뒀으니, 실사용해보고 유입 이탈이 크면 위 대안 중 하나로 조정하는 걸 권장.

### 검증
`npx tsc --noEmit`/`npx eslint .`/`npm run build`/`npx vitest run`(39개, 실제 Firebase 프로젝트에 붙는 통합 테스트 포함) 전부 통과. 로컬 dev 서버로 `/`, `/community`(4가지 sort 파라미터 전부), `/my`, `/rankings`, `/map`, `/api/search` 전부 200 응답 및 HTML에 신규 섹션(하단 탭바, 정렬 드롭다운, 검색창, 급상승 위젯 등) 정상 렌더링 확인. 커뮤니티/랭킹 데이터가 아직 없는 상태라 정렬 로직 자체는 유닛 테스트(빌드 타임 타입체크 + 로직 리뷰) 수준으로 검증했고, 실제 다건 데이터로 정렬 순서까지 눈으로 보는 E2E는 다음에 실 데이터 쌓이면 재확인 권장.