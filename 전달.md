프롬프트 11 — 지역별 찜 밀도 지도 뷰
스푼노트에 지역별 찜(커뮤니티 카운트) 밀도를 한눈에 보여주는 지도 화면을 추가하려고 해. 별도 하단 탭이 아니라 홈 탭에서 진입하는 화면으로 만들어줘.

요구사항:
1. 기존에 쓰고 있는 지도 라이브러리(Leaflet)를 그대로 활용
2. 지도를 열면 전체 지역이 보이는 줌 레벨에서 시작, 각 동/구 단위로 그 지역에 속한 canonical place들의 찜 카운트 합계를 계산해서 원형 버블 마커로 표시 — 카운트가 많을수록 버블 크기/색을 진하게(히트맵 느낌)
3. 버블을 누르면 해당 지역의 랭킹 리스트(기존 지역 랭킹 페이지)로 이동
4. 지도를 확대하면 지역 단위 버블 대신 개별 장소 마커로 전환 (기존 보드 상세 화면의 지도 마커 로직과 유사하게, 임계값 이상인 장소는 금색)
5. 지역 단위 집계를 매 요청마다 실시간 계산하면 비용 부담이 클 수 있으니, 캐싱 또는 주기적 배치 집계 방식을 설계해서 반영해줘(예: 지역별 카운트를 별도 문서에 캐시하고 일정 주기로 갱신, 또는 찜 카운트 변경 시 해당 지역 캐시만 갱신)

완료 후 build/lint 통과 확인해줘.

---

## 구현 완료 (2026-08-27, Claude Code)

기존 코드부터 파악: `MapView.tsx`(보드 상세 지도)가 이미 react-leaflet 5 + `L.divIcon`
커스텀 마커(금색=임계값 이상) 패턴을 갖고 있어서 그 스타일을 그대로 개별 장소 마커에
재사용함. leaflet/react-leaflet/@types/leaflet 전부 이미 설치돼 있어서 새 의존성은 없음.

### 만든 파일
- `components/map/DensityMapView.tsx` — 실제 지도(client, default export). 낮은 줌은
  지역별 `CircleMarker` 버블(반경·투명도가 찜 합계에 비례), `MAP.placeZoomThreshold`(13)
  이상으로 확대하면 개별 장소 `Marker`로 전환(`useMapEvents`로 줌 변화 추적). 버블 클릭 →
  `/rankings?region=X`, 장소 마커 팝업에도 같은 링크
- `components/map/DensityMapViewLoader.tsx` — `MapViewLoader.tsx`와 동일한 `dynamic(...,
  {ssr:false})` 패턴

### 고친 파일
- **`app/map/page.tsx`** — 프롬프트 10 때 만든 자리표시 페이지를 실제 기능으로 교체
- **`lib/services/canonicalPlaceService.ts`** — 요구사항 5(캐싱) 핵심:
  - 새 컬렉션 `regionStats/{region}` 신설 - `{region, totalSaveCount, latSum, lngSum,
    coordCount, updatedAt}`
  - `recordPlaceSave`/`removePlaceSave`가 saveCount를 실제로 늘리거나 줄이는 순간마다
    `incrementRegionStat`으로 이 캐시 문서만 증분 갱신(fail-open, `set({...},{merge:true})`
    + `FieldValue.increment`라 문서가 없어도 안전하게 생성됨). 좌표 합산(latSum/lngSum/
    coordCount)은 canonical place가 "새로" 만들어질 때만 더해서 centroid 중복 집계 방지
  - `listRegionDensity()` — 지도 낮은 줌용, `regionStats`만 읽음(canonicalPlaces 전체
    스캔·재집계 없음 - 이게 요구사항 5의 핵심)
  - `listMapPlaces()` — 지도 높은 줌용, 찜된(saveCount>0) canonical place 전체를 좌표와
    함께 반환(지역 필터 없이 한 번에, MVP 규모 전제 - 개별 장소 목록은 집계가 아니라 단순
    조회라 캐싱 대상에서 제외함, 근거는 아래 "설계 결정" 참고)
  - `backfillRegionStats()` — `boardService.migratePublicVisibility()`와 같은 "1회성 수동
    backfill" 패턴. 이 기능 배포 전에 이미 있던 canonicalPlaces 데이터는 캐시에 자동으로
    안 채워지니, canonicalPlaces 전체를 다시 합산해서 `regionStats`를 통째로 덮어씀
    (increment 아닌 set이라 여러 번 실행해도 항상 같은 결과)
- `lib/types.ts` — `RegionDensity`, `MapPlace` 타입 추가
- `lib/constants.ts` — `MAP` 설정값(`placeZoomThreshold`, `fallbackCenter/Zoom`,
  버블 반경 범위)

### 설계 결정
- **요구사항 5 캐싱 범위를 "지역 집계"로 한정함**: "지역 단위 집계를 매 요청마다 실시간
  계산하면 비용 부담"이라는 문구를 문자 그대로 읽어서, 무거운 부분(canonicalPlaces
  전체를 region별로 합산하는 집계 연산)만 캐시했음. 반면 `listMapPlaces()`(개별 장소
  목록)는 집계가 아니라 단순 필터링 조회라 기존 `listRankedPlaces`/`listRegionCentroids`와
  같은 수준의 비용이고, 이미 이 프로젝트 전반에서 "MVP 규모면 매번 조회, 커지면 캐싱"
  원칙을 쓰고 있어서 캐싱 대상에서 제외함. 데이터가 늘어나면 지도 바운딩 박스 기준
  쿼리로 바꾸는 게 다음 단계일 것
- **캐시 갱신은 배치가 아니라 이벤트 기반(요구사항 5의 두 옵션 중 후자)**: 이 리포에
  아직 크론/배치 인프라가 없어서, "찜 카운트 변경 시 해당 지역 캐시만 갱신"을 택함 -
  `recordPlaceSave`/`removePlaceSave`가 이미 saveCount를 바꾸는 유일한 두 경로라 거기에
  거는 게 자연스럽고, 새 인프라(cron)도 필요 없음
- **버블 크기/색은 상대값**: 이번 요청에서 나온 지역들 중 최댓값 기준으로 정규화(절대
  기준값이 없어서). 지역이 하나뿐이면 그 지역이 항상 최대 크기로 나옴 - 지역이 많아지면
  자연스럽게 상대적 크기 차이가 드러남
- **개별 장소 마커 팝업엔 "담아가기"를 안 넣음**: 지도는 탐색/발견 목적이라 팝업에서
  바로 담게 하려면 CollectModal을 지도 위에 띄워야 하는데, 지금은 그 대신 "OO 랭킹
  보기"로 랭킹 페이지(담아가기 버튼 있음)로 보냄 - 스코프를 좁게 유지. 필요하면 후속
  작업으로 지도 팝업에도 담아가기를 붙일 수 있음

### 검증
`npx tsc --noEmit`/`npx eslint .`/`npm run build`/`npx vitest run`(39개) 전부 통과.
로컬 dev 서버로 `/map` 200 확인(SSR에서는 leaflet 로딩 placeholder만 보이는 게 정상 -
`MapViewLoader`와 동일한 `ssr:false` 구조). `backfillRegionStats()`를 실제 프로젝트에
한 번 실행해봤는데 지금 커뮤니티 데이터가 하나도 없어서(이전 테스트 데이터 전부 정리된
상태) 0개 지역이 나온 것까지 확인 - 정상 동작이고, 실제로 커뮤니티공개 보드에 장소가
쌓이면 다음 저장부터 `regionStats`가 자동으로 채워짐. 이 세션엔 브라우저 자동화 도구가
없어서 지도 버블/줌 전환의 실제 렌더링은 코드 리뷰 + 타입체크 수준으로 검증했고, 실
데이터가 쌓이면 브라우저로 직접 확인 권장.